# 本站第三方整合設定。複製為 .env 並填入實際值。
# 全部為 PUBLIC_ 前綴：build 時注入、會出現在 client 端(這些值本就公開)。
# 任一功能的變數未設定時，該功能不渲染，站台照常運作(gating)。

# ---- Giscus 留言(P2-09) ----
# 四個值皆從 https://giscus.app 設定精靈取得(選好 repo、Discussion category 後產生)。
# 四個任一為空 → 文章頁不顯示留言區。
PUBLIC_GISCUS_REPO=owner/repo
PUBLIC_GISCUS_REPO_ID=
PUBLIC_GISCUS_CATEGORY=
PUBLIC_GISCUS_CATEGORY_ID=

# ---- Cloudflare Web Analytics(P2-10，無 cookie、隱私友善) ----
# 從 Cloudflare dashboard → Web Analytics 建立站點後取得的 beacon token。
# 為空 → 不輸出 analytics beacon。
PUBLIC_CF_BEACON_TOKEN=

# ---- Webmention / IndieWeb(P2-11) ----
# 在 https://webmention.io 用自有 domain 登入(IndieAuth)後註冊的網域，不含 scheme。
# 為空 → 不輸出 webmention endpoint、文章頁不抓留言(microformats2 標記仍照常輸出)。
PUBLIC_WEBMENTION_DOMAIN=jimmychen.me

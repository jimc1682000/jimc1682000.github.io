#!/usr/bin/env bash
# Résumé deterministic guardrails. Enable once with:
#   git config core.hooksPath .githooks
set -euo pipefail
repo="$(git rev-parse --show-toplevel)"
node "$repo/resume/validate.mjs"
